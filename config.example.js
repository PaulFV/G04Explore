/* Lokale Entwicklung: als config.js kopieren und den eigenen eingeschränkten
 * Google-Maps-Browser-Key eintragen. Die echte config.js wird beim GitHub-Pages-
 * Deployment automatisch aus dem Repository-Secret erzeugt und nicht committed.
 *
 * googleMapId ist optional. Ohne eigene Map-ID nutzt die Karte "DEMO_MAP_ID",
 * die Google nur für Tests vorsieht.
 */
window.G04_CONFIG = {
  googleMapsKey: "",
  googleMapId: "",
};
